'use strict';

const {} = require('express-handler-loader')('all');

const {} = require('express-handler-loader')('ufwd');

const {} = require('express-handler-loader')('ufwd_activity');

const router = module.exports = require('express').Router();